{{#each posts as |post| }}
<div class="row align-center mb-5">
  <div class="col-md-6">
    <h2>{{this.title}}</h2>
    <p>{{this.content}}</p>
    <p>{{this.username}}</p>
  </div>
  <div class="col-md-6">
        <h3>Date created on: {{this.created_at}}</h3>
  </div>
</div>
{{/each}}